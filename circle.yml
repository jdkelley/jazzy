machine:
  xcode:
    version: "8.2"
  environment:
    XCODE_WORKSPACE: SourceKitten/SourceKitten.xcworkspace

checkout:
  post:
    - git submodule update --init --recursive

test:
  override:
    - bundle exec rake spec
    - bundle exec danger --verbose || true
